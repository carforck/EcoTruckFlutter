name: flutter_application_1
description: A new Flutter project.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Logging profesional
  logger: ^2.0.2

  # HTTP para conexión con backend
  http: ^1.2.1

  # Tipografía elegante
  google_fonts: ^6.1.0

  # SVG para logos vectoriales
  flutter_svg: ^2.0.9

  # Mapa compatible con Flutter Web
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

  # Geolocalización en tiempo real
  geolocator: ^10.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logoecotruck.jpg
